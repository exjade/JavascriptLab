const boolean1 = true;
const boolean2 = false;

/*
    Si comparamos los dos BOOLEANS nos dará FALSE porque el tipo de dato es el mismo pero el VALOR no lo es.
*/

console.log(boolean1 === boolean2);

console.log(boolean1 === true);

// El tipo de dato no es igual como al igual que ocurre con NUMEROS y STRINGS

console.log(boolean1 === "true"); // Nos da error porque es una STRING y no un BOOLEAN